# chess-opening-training-flow
Create an interactive training flow that allows a user to select a chess opening, practice the first N moves (default: 10), and validates the user’s moves against all allowed opening variations in correct order.
